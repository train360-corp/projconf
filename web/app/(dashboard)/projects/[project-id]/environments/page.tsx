export default async function Page() {
  return (
    <div className={"w-full h-full flex flex-col items-center justify-center"}>
      <p className={"text-muted"}>{"No Environment Selected"}</p>
    </div>
  )
}